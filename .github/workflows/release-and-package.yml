name: Repository Dispatch
on:
  push:
    branches: main
  
jobs:
  build:
    #if: github.event.action == 'closed' && github.event.pull_request.merged == true
    runs-on: windows-latest

    steps:
      - name: 🛎 Checkout repo
        uses: actions/checkout@v2
        with: 
          fetch-depth: 0
          
      - name: 💾 7Zip library
        run: cd ./library
             7z a -t7z -mx=9 MMLibrary.7z DynTools\GhTools\
